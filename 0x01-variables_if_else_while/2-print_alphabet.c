#include <stdio.h>

/**
 * main - prints alphabet in lower case
 * Return: Always 0
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++);
	{
		putchar(lc);
	}
	putchr('\n');
			return (0;
