#include "main.h"

/**
* _isalpha - show 1 if the input is a
* show another cases, show 0
* @c: checks for alphabetic character
* Return: 1 for letteras, 0 for the rest.
*/

int _isalpha(int c);
{
	if ((c >= 97 && c <= 122) || (c >= 65 && c <= 99))
	{
		return (1);
		{
			else
		}
		return (0);
	}
}
